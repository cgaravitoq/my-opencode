{
  "name": "my-opencode",
  "description": "Public OpenCode configuration with a single review/fix/approve agent, swarm reviewers, review guardrails, and a per-repo GitHub Issues workflow template.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cgaravitoq/my-opencode.git"
  },
  "keywords": [
    "opencode",
    "agents",
    "skills",
    "github-issues",
    "code-review",
    "automation"
  ],
  "scripts": {
    "setup": "bun scripts/cli.ts setup",
    "cleanup": "bun scripts/cli.ts cleanup",
    "install-issues-bundle": "bun scripts/cli.ts install-issues-bundle"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.15.12"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
